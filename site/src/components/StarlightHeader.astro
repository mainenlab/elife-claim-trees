---
// Custom Starlight header — matches the existing site's nav (Layout.astro).
// Starlight's default Header renders title + search + theme toggle. We
// replace it with the same sticky 12px-tall nav the rest of the site uses,
// plus the search and theme toggle off to the right.

import Search from '@astrojs/starlight/components/Search.astro';
import SocialIcons from '@astrojs/starlight/components/SocialIcons.astro';

const base = import.meta.env.BASE_URL.replace(/\/$/, '');
const url = (path: string) => `${base}${path}/`;

// Active-link highlighting based on the current path.
const pathname = Astro.url.pathname.replace(base, '') || '/';
const isActive = (path: string) =>
  path === '/'
    ? pathname === '/' || pathname === ''
    : pathname.startsWith(path);
---

<script is:inline>document.documentElement.dataset.theme = 'light';</script>
<div class="site-nav-wrapper">
  <nav class="site-nav">
    <div class="site-nav-inner">
      <a href={url('')} class="site-brand">eLife Claim Trees</a>
      <a href={url('/papers')} class={`site-nav-link ${isActive('/papers') ? 'active' : ''}`}>Papers</a>
      <a href={url('/methodology')} class={`site-nav-link ${isActive('/methodology') ? 'active' : ''}`}>Methodology</a>
      <a href={url('/findings')} class={`site-nav-link ${isActive('/findings') ? 'active' : ''}`}>Findings</a>
      <a href={url('/docs')} class={`site-nav-link ${isActive('/docs') ? 'active' : ''}`}>Docs</a>
      <div class="site-nav-spacer"></div>
      <div class="site-nav-actions">
        <Search {...Astro.props} />
        <SocialIcons {...Astro.props} />
      </div>
    </div>
  </nav>
</div>

<style>
  .site-nav-wrapper {
    position: sticky;
    top: 0;
    z-index: 40;
    background: #ffffff;
    border-bottom: 1px solid #f3f4f6;
  }

  .site-nav {
    width: 100%;
  }

  .site-nav-inner {
    max-width: 80rem;            /* matches max-w-7xl */
    margin: 0 auto;
    padding: 0 1rem;
    height: 3rem;                 /* matches h-12 */
    display: flex;
    align-items: center;
    gap: 1.5rem;                  /* matches gap-6 */
    font-family: Inter, system-ui, -apple-system, sans-serif;
  }

  .site-brand {
    font-weight: 600;
    font-size: 0.875rem;          /* text-sm */
    color: #111827;               /* gray-900 */
    text-decoration: none;
  }

  .site-brand:hover {
    color: #374151;               /* gray-700 */
  }

  .site-nav-link {
    font-size: 0.875rem;
    color: #6b7280;               /* gray-500 */
    text-decoration: none;
  }

  .site-nav-link:hover {
    color: #374151;               /* gray-700 */
  }

  .site-nav-link.active {
    color: #111827;
    font-weight: 500;
  }

  .site-nav-spacer {
    flex: 1;
  }

  .site-nav-actions {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    font-size: 0.875rem;
  }

  /* Make the Starlight search button look minimal and match the nav text */
  .site-nav-actions :global(button) {
    border: 1px solid #e5e7eb;
    border-radius: 4px;
    padding: 4px 8px;
    background: #ffffff;
    color: #6b7280;
    font-size: 0.75rem;
    font-family: Inter, system-ui, -apple-system, sans-serif;
    cursor: pointer;
  }

  .site-nav-actions :global(button:hover) {
    background: #f9fafb;
    color: #111827;
  }
</style>
